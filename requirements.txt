# E.V3 Core Dependencies
# Install with: pip install -r requirements.txt
# Note: llama-cpp-python should be installed separately (see README)

# === Core Framework ===
PySide6>=6.6.0              # Qt6 GUI framework for UI
PyOpenGL>=3.1.7             # OpenGL for 3D rendering
numpy>=1.24.0               # Array operations (also required by llama-cpp)
loguru>=0.7.0               # Enhanced logging
pyyaml>=6.0.1               # YAML configuration

# === Windows Integration ===
pywin32>=306                # Windows API (named pipes, system tray, hotkeys)
pynput>=1.7.6               # Global hotkey support

# === 3D Model Loading ===
pygltflib>=1.16.0           # GLTF/GLB/VRM model loading
Pillow>=10.0.0              # Image/texture loading

# === Optional: Audio Support ===
# pygame>=2.5.0             # Audio playback for TTS (uncomment if needed)

# === Optional: Neural TTS ===
# onnxruntime>=1.16.0       # For Piper TTS models (uncomment if needed)

# === LLM Support (INSTALL SEPARATELY) ===
# llama-cpp-python>=0.3.0   # Local LLM inference
#
# Install with one of:
#   pip install llama-cpp-python                    # CPU-only
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121  # CUDA
#
# Or use pre-built wheel from repo (if available):
#   pip install llama_cpp_python-0.3.2-cp313-cp313-win_amd64.whl

# === Optional: External LLM API ===
# openai>=1.0.0             # For GPT API (uncomment if using external LLM)

# === Optional: Calendar Integration ===
# O365>=2.0.27              # Microsoft 365/Outlook (uncomment if needed)
# google-auth>=2.23.0       # Google Calendar (uncomment if needed)
# google-auth-oauthlib>=1.1.0
# google-api-python-client>=2.100.0

# === Development Dependencies (optional) ===
# pytest>=7.4.0             # Testing framework
# black>=23.0.0             # Code formatting
# pylint>=3.0.0             # Linting
soundfile>=0.12.1

# TTS engines (optional - install as needed)
# piper-tts>=1.2.0  # Lightweight neural TTS (recommended)
# TTS>=0.20.0  # Coqui TTS (more advanced, larger)
# pyttsx3>=2.90  # Basic TTS fallback

# Global hotkey support
keyboard>=0.13.5

# Utilities
pydantic>=2.0.0  # Data validation
rich>=13.6.0  # Beautiful terminal output

# Building (optional - for creating executables)
pyinstaller>=6.0.0  # Create standalone executables
